import { StyleSheet, Text, SafeAreaView } from 'react-native'
import React from 'react'
import TextInput1 from './src/TextInput'

const App = () => {
  return (
    <SafeAreaView>
      <TextInput1/>
    </SafeAreaView>
  )
}

export default App

const styles = StyleSheet.create({})